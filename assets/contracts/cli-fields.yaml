commands:
  serve: serve
  access: access
access:
  bootstrap: bootstrap
flags:
  output: --output
  config: --config
serviceKey:
  rolePlatformAdmin: platform-admin
  keyBytes: 32
  hashCost: 12
outputs:
  text: text
  json: json
environment:
  gatewayConfig: LIAPOLDUS_GATEWAY_CONFIG
paths:
  defaultConfig: /etc/liapoldus/gateway.yaml
codes:
  accessBootstrapConflict: access_bootstrap_conflict
  configNotFound: config_not_found
  configInvalid: config_invalid
exits:
  ok: 0
  internal: 1
  arguments: 2
  validation: 3
  conflict: 4
  unavailable: 7
sources:
  flag: flag
  environment: environment
  system: system
json:
  ok: ok
  problem: problem
  code: code
  detail: detail
  token: token
text:
  ok: ok
diagnostics:
  commandExpected: ожидается команда serve или access bootstrap
  configNotFound: Файл конфигурации не найден.
  configInvalid: Конфигурация не прошла проверку.
  outputInvalid: --output должен иметь значение text или json
  configRequired: --config требует путь к gateway.yaml
  configLookupFailed: gateway.yaml не найден или является каталогом
  accessBootstrapConflict: активный ключ доступа уже существует
