paths:
  healthz: /healthz
  status: /api/status
  plugins: /api/plugins
  adminSurfaces: /api/plugins/admin-surfaces
  adminPages: admin/pages
  restart: restart
  operations: /api/operations
  audit: /api/audit
  groups: /api/groups
  groupByID: /api/groups/
  groupIDSeparator: "/"
  groupIDPattern: '^[a-z][a-z0-9-]{0,62}$'
methods:
  get: GET
  post: POST
json:
  requestId: requestId
  operationId: operationId
  state: state
  items: items
  nextCursor: nextCursor
  id: id
  idempotencyKey: idempotencyKey
  replyTo: replyTo
  status: status
  currentRevision: currentRevision
  previousRevision: previousRevision
  createdAt: createdAt
  startedAt: startedAt
  finishedAt: finishedAt
  result: result
  problem: problem
  timestamp: timestamp
  actor: actor
  action: action
  resource: resource
  digestBefore: digestBefore
  digestAfter: digestAfter
  name: name
  type: type
  address: address
  activeConnections: activeConnections
  healthy: healthy
  capabilities: capabilities
  limits: limits
  health: health
  profile: profile
  domain: domain
  serial: serial
  notAfter: notAfter
  validity: validity
  diagnostics: diagnostics
  limit: limit
  cursor: cursor
  kind: kind
  active: active
  caddy: caddy
  variant: variant
  buildId: buildId
  modules: modules
  drift: drift
  runtimeDigest: runtimeDigest
  compositionDigest: compositionDigest
  dataPlaneReadiness: dataPlaneReadiness
  reason: reason
headers:
  ifMatch: If-Match
  requestId: X-Request-ID
  location: Location
  retryAfter: Retry-After
idempotency:
  keyChars: 128
  keyMin: 16
contentTypes:
  yaml: application/yaml
  json: application/json
  problem: application/problem+json
  text: text/plain
statuses:
  ok: ok
  ready: ready
  draining: draining
  failed: failed
  invalid: invalid
  publishing: publishing
  healthy: healthy
  degraded: degraded
  unavailable: unavailable
  starting: starting
  unhealthy: unhealthy
  stopped: stopped
  renewing: renewing
  pending: pending
  running: running
  succeeded: succeeded
  empty: empty
  notReady: not-ready
  systemReleaseRequired: system-release-required
  caddyUnavailable: caddy-unavailable
  recoveryRequired: recovery-required
codes:
  bearerRequired: management_bearer_required
  routeNotFound: route_not_found
  registryUnavailable: registry_unavailable
  mtlsRequired: mtls_required
  pluginUnavailable: plugin_unavailable
  groupNotFound: group_not_found
  groupAlreadyExists: group_already_exists
  invalidRequest: invalid_request
