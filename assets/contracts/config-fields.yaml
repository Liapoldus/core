root:
  - includes
  - variables
  - secrets
  - registry
  - sites
  - listeners
  - upstreams
  - tlsProfiles
  - tlsIssuers
  - authPolicies
  - dataProviders
  - wafPolicies
  - rateLimits
  - captchaProviders
  - plugins
  - management
  - logging
  - metrics
  - tracing
listener:
  - type
  - address
  - tls
  - routes
  - rules
  - limits
includes: includes
listeners: listeners
sites: sites
secrets: secrets
variables: variables
substitution:
  open: ${
  close: '}'
semantics:
  unusedSite: unused
  overriddenSite: overridden
runtime:
  http: http
  directory: directory
  listener:
    type: type
    address: address
    routes: routes
  site:
    source: source
    type: type
    root: root
    index: index
    indexDefault: index.html
    manifestFileName: site.yaml
  route:
    when: when
    then: then
    path: path
    prefix: prefix
    exact: exact
    regex: regex
    site: site
