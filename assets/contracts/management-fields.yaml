paths:
  healthz: /healthz
  status: /api/status
  config: /api/config
  configValidate: /api/config/validate
  configReload: /api/config/reload
  reload: /api/reload
  sites: /api/sites
  publish: publish
  rollback: rollback
  listeners: /api/listeners
  upstreams: /api/upstreams
  plugins: /api/plugins
  adminSurfaces: /api/plugins/admin-surfaces
  adminPages: admin/pages
  restart: restart
  logs: logs
  tls: /api/tls
  renew: renew
  revoke: revoke
  operations: /api/operations
  audit: /api/audit
  metrics: /metrics
methods:
  get: GET
  post: POST
  put: PUT
  delete: DELETE
json:
  requestId: requestId
  operationId: operationId
  state: state
  items: items
  nextCursor: nextCursor
  yaml: yaml
  revision: revision
  digest: digest
  valid: valid
  source: source
  idempotencyKey: idempotencyKey
  expectedCurrentRevision: expectedCurrentRevision
  expectedRevision: expectedRevision
  id: id
  replyTo: replyTo
  status: status
  slug: slug
  route: route
  root: root
  currentRevision: currentRevision
  previousRevision: previousRevision
  createdAt: createdAt
  startedAt: startedAt
  finishedAt: finishedAt
  result: result
  problem: problem
  timestamp: timestamp
  actor: actor
  action: action
  resource: resource
  digestBefore: digestBefore
  digestAfter: digestAfter
  name: name
  type: type
  address: address
  activeConnections: activeConnections
  healthy: healthy
  capabilities: capabilities
  limits: limits
  health: health
  profile: profile
  domain: domain
  serial: serial
  notAfter: notAfter
  validity: validity
  diagnostics: diagnostics
  limit: limit
  cursor: cursor
headers:
  ifMatch: If-Match
  requestId: X-Request-ID
  location: Location
  retryAfter: Retry-After
contentTypes:
  yaml: application/yaml
  json: application/json
  problem: application/problem+json
  text: text/plain
statuses:
  ok: ok
  ready: ready
  draining: draining
  failed: failed
  invalid: invalid
  publishing: publishing
  healthy: healthy
  degraded: degraded
  unavailable: unavailable
  starting: starting
  unhealthy: unhealthy
  stopped: stopped
  renewing: renewing
  pending: pending
  running: running
  succeeded: succeeded
idempotency:
  limitDefault: 20
  limitMax: 100
  limitMin: 1
  keyChars: 128
  keyMin: 16
  window: 24h
  key: X-Idempotency-Key
  replyTo: Reply-To
codes:
  routeNotFound: route_not_found
  siteSourceImmutable: site_source_immutable
  idempotencyConflict: idempotency_conflict
  releaseInvalid: release_invalid
  registryUnavailable: registry_unavailable
  noPreviousRelease: no_previous_release
  releaseRevisionConflict: release_revision_conflict
pagination:
  limitDefault: 50
  limitMax: 100
  limitMin: 1
operationState:
  done: done
  accepted: accepted
