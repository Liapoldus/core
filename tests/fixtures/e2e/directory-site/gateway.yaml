registry:
  path: ./registry
sites:
  docs:
    source:
      type: directory
      root: ./site
listeners:
  web:
    type: http
    address: __ADDRESS__
    routes:
      - when:
          path: { prefix: / }
        then:
          site: docs
