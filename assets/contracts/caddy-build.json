{
  "adapter": "caddyfile",
  "diagnostics": {
    "adapterUnavailable": "Встроенный Caddy config adapter недоступен.",
    "runtimeAlreadyActive": "Встроенный Caddy runtime уже запущен.",
    "runtimeNotActive": "Встроенный Caddy runtime не запущен."
  },
  "requiredModules": [
    "http",
    "http.handlers.file_server",
    "http.handlers.reverse_proxy",
    "layer4",
    "layer4.handlers.proxy"
  ]
}
